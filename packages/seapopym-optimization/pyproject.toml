[project]
name = "seapopym-optimization"
version = "0.1.1"
description = "A Python package for optimizing SeapoPym models using CMA-ES or a genetic algorithm (DEAP), with Dask"
authors = [
    { name = "Jules Lehodey", email = "lehodey.jules+seapopym@gmail.com" }
]
readme = "README.md"
license = { text = "GPL-3.0-or-later" }
requires-python = ">=3.12,<3.13"

dependencies = [
    "seapopym",
    "cma>=3.3",
    "deap>=1.4.3",
    "dask>=2025.9.0",
    "distributed>=2025.9.0",
    "pandera>=0.26.1",
    "scipy>=1.16.2",
    "scikit-learn>=1.7.2",
    "salib>=1.5.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
seapopym = { workspace = true }
