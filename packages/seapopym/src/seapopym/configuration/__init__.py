"""This module contains the configuration structures for the different models.

It handles parameter validation, parsing, and organization into logical units
(forcing, functional groups, kernel parameters).
"""
